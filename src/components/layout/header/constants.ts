/** Cỡ icon trong header */
export const ICON_SIZE = {
  action: 20,
  search: 18,
  caret: 16,
} as const;

/** Input chỉ chừa 2.25rem cho rightIcon, không đủ cho ⌘K */
export const SEARCH_PADDING_RIGHT = "4rem";
